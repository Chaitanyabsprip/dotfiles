#!/bin/sh

workdirs="$(
	(
		fd . -HIatd -d 1 -E .git \
			~/programs \
			~/projects
		fd . -HIad 1 -E .git ~/.config
		echo "$HOME"/dotfiles
		echo "$HOME"/projects
		echo "$HOME"/programs
		echo "$SCRIPTS"
		echo "$DOWNLOADS"
	) | sort -u
)"
echo "$workdirs"
