#!/bin/sh

_have() { type "$1" >/dev/null 2>&1; }

! _have mods && echo "requires github.com/charmbracelet/mods" && exit 1

aicommand="mods -C --status-text Ummm $*"
if [ -p /dev/stdin ]; then $aicommand </dev/stdin; else $aicommand; fi
